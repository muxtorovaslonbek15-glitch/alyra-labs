# Server-only. Get a key at https://console.groq.com/
# Used for lab tutor (/api/explain) and vision OCR (/api/ocr).
GROQ_API_KEY=

# Alyra Master Perfumer backend (ZPL_BACKEND /api/perfumer)
# Local default (ZPL backend; chemistry Next often takes 3000/3001):
NEXT_PUBLIC_PERFUMER_API_URL=https://www.zorastrianpremierleague.in/api/perfumer
# Local override: http://localhost:3001/api/perfumer

# Firebase client (Alyra Labs) — from Firebase console
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin (server) — required for /api/explain, /api/ocr, /api/progress
# Option A: full service account JSON (one line)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Option B: split fields (private key may use \n for newlines)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# Sentry (optional but recommended for soft launch)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
# Optional: upload source maps in CI/Vercel
SENTRY_AUTH_TOKEN=
